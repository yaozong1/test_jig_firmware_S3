# K10-3U8 数据格式调试

## 当前测试

**连接**：AIN1 = 5V, AGND = GND

**原始响应数据**：
```
01 03 10 00 01 00 03 00 00 00 00 00 00 00 00 00 00 00 00 20 9a
```

**数据解析**：
- CH1 原始值: 0x0001 = 1 (十进制)
- CH2 原始值: 0x0003 = 3 (十进制)

## 可能的换算关系

如果 AIN1 实际电压是 5V (5000 mV)：

| 原始值 | 缩放因子 | 计算值 | 是否合理 |
|--------|---------|--------|---------|
| 1 | ×1 | 1 mV | ❌ 太小 |
| 1 | ×10 | 10 mV | ❌ 太小 |
| 1 | ×100 | 100 mV | ❌ 太小 |
| 1 | ×1000 | 1000 mV (1V) | ❌ 太小 |
| 1 | ×5000 | 5000 mV (5V) | ✅ **可能正确！** |

## K10-3U8 常见数据格式

根据类似模块的规格：

### 可能性 1: 数据单位是 0.005V
- 原始值 1 = 0.005V = 5mV
- 5V = 1000 个单位
- **但实际只有 1，不对**

### 可能性 2: 模块量程设置问题
K10-3U8 可能有不同的量程设置：
- 0-5V 量程
- 0-10V 量程
- 0-30V 量程

如果设置为 **0-30V 量程**：
- 满量程 30V 对应 65535 (16位)
- 单位 = 30000mV / 65535 ≈ 0.458 mV/单位
- 5V 应该约为 5000 / 0.458 ≈ 10917
- **但实际只有 1，还是不对**

### 可能性 3: 数据格式是 0.001V (实际测试)
让我查看原始数据更仔细：
```
00 01  -> 1
00 03  -> 3
```

如果 CH2 也有电压输入...等等，CH2 为什么有值？

## 需要确认的问题

### 问题 1: CH2 为什么有值 (3)?
- AIN2 是否也连接了什么？
- 还是这是模块的零点漂移？

### 问题 2: 模块配置
K10-3U8 可能需要配置：
- 量程选择寄存器
- 校准寄存器
- 单位设置寄存器

## 调试步骤

### 1. 尝试不同的输入电压
连接 AIN1 到不同电压，观察原始值变化：
- 3.3V → 原始值应该是多少？
- 10V → 原始值应该是多少？
- 12V → 原始值应该是多少？

### 2. 检查模块手册
- 确认寄存器地址 0x0000-0x0007 是否是电压值
- 确认数据单位
- 检查是否有配置寄存器

### 3. 可能的线性关系
如果 5V → 1，那么：
- 缩放因子 = 5000 / 1 = 5000
- 实际电压(mV) = 原始值 × 5000

但这意味着最大只能测 65535 × 5000 = 327,675,000 mV = 327V，太大了。

## 最可能的情况

**K10-3U8 的数据格式可能是 0.01V 单位**：
- 原始值 1 = 0.01V = 10mV
- 5V = 500 个单位
- **但实际测量到 1，不是 500**

## 立即执行的测试

重新编译烧录后，新固件会显示：
```
CH1: Raw=0x0001 (1)
     ×1=1 mV, ×10=10 mV, ×100=100 mV, ×1000=1000 mV, ×5000=5000 mV
```

**请告诉我**：
1. CH1 的 ×5000 值是 5000 mV 吗？（接近实际 5V）
2. CH2 为什么有值 3？AIN2 连接了什么？
3. 如果有条件，测试 AIN1=3.3V 时的原始值是多少

## 编译命令

```powershell
cd C:\Users\h1576\Desktop\Roam\pe_code\pe_board\tools\test_jig_firmware
idf.py build
idf.py -p COM24 flash monitor
```
