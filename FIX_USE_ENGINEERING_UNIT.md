# K10-3U8 工程量vs过程量 - 最终修复

## ❌ 问题原因

之前使用了 **过程量寄存器** (0x0020-0x0027)，数据格式是 0~32768，需要换算。
但这导致了以下问题：
1. 换算误差
2. 数据不稳定
3. 出现异常值 (0xFFF0)

## ✅ 正确方案：使用工程量寄存器

根据 K10-3U8 文档 **4.3.3 节**：

### 工程量特性
- **寄存器地址**: 40017-40024 (0x0010-0x0017)
- **数据格式**: **直接返回 mV 值**
- **量程**: 0~10V → 0~10000 mV
- **单位**: 模拟量输入单位为 **mV**
- **优点**: 
  - ✅ 无需换算
  - ✅ 精度高
  - ✅ 数据稳定
  - ✅ 易于理解

### 示例（从文档）
当 AI1 = 9955mV (9.955V) 时：
- **发送**: 01 03 00 10 00 04 45 CC
  - 地址: 0x0010
  - 读取 4 个寄存器
  
- **接收**: 01 03 08 26 E3 00 00 13 88 00 00 FB D8
  - AI1 值: 0x26E3 = 9955 (mV) ✅
  - AI2 值: 0x0000 = 0
  - AI3 值: 0x1388 = 5000 (mV) = 5V
  - AI4 值: 0x0000 = 0

## 🔧 代码修改

### 1. 寄存器地址
```c
// 修改前（过程量）
tx_buf[3] = 0x20;  // Start address 0x0020 ❌

// 修改后（工程量）
tx_buf[3] = 0x10;  // Start address 0x0010 ✅
```

### 2. 数据解析
```c
// 修改前（需要换算）
uint32_t voltage_mv = ((uint32_t)raw * 10000) / 32768; ❌

// 修改后（直接读取）
voltages[i] = raw;  // 已经是 mV 值 ✅
```

## 📊 预期结果

重新编译烧录后，当 AI1 = 5V 时：

```
I (xxx) VADC: >>> Sending Modbus RTU request (8 bytes):
I (xxx) VADC:     Addr=0x01 Func=0x03 Start=0x0010 Count=8
I (xxx) VADC: 01 03 00 10 00 08 45 CC  ← 地址 0x0010
...
I (xxx) VADC: === Voltage Values (工程量，单位 mV) ===
I (xxx) VADC:   AI1: Raw= 5000 (0x1388) =  5000 mV (  5.000 V)  ← 正确！
I (xxx) VADC:   AI2: Raw=    0 (0x0000) =     0 mV (  0.000 V)
...
VOLTAGE_ADC: {"ch1":5000,"ch2":0,...}
```

## 🎯 关键点

1. **工程量 vs 过程量**：
   - 工程量：0~10V → 0~10000 (mV) - **推荐** ✅
   - 过程量：0~10V → 0~32768 (需换算) - 不推荐

2. **寄存器地址**：
   - 工程量：0x0010 (40017) 开始
   - 过程量：0x0020 (40033) 开始

3. **数据单位**：
   - 工程量：mV（毫伏）
   - 过程量：无单位数字

4. **精度**：
   - 工程量：0.1V (100mV) 精度
   - 过程量：约 0.3mV 精度（换算后）

## 🚀 编译和测试

```powershell
cd C:\Users\h1576\Desktop\Roam\pe_code\pe_board\tools\test_jig_firmware
idf.py build
idf.py -p COM24 flash monitor
```

现在应该能正确读取 AI1=5V 的值了！
